# Task 04 — はっけんアルバムを「おとのずかん」へ変更する

## 状態

完了。Issue #129で、図鑑エントリの表示構造、未発見シルエット、発見済みの生き物と
学び、レスポンシブ表示を実装した。PR #131で`main`へ統合し、2026-08-04に
Productionの「おとのずかん」で表示を確認した。

## 目的

既存の `/discoveries` を、未発見は固有シルエット、発見済みは生き物の全身と
学びを表示する図鑑体験へ変更する。

## 先に読むもの

- `AGENTS.md`
- `docs/sound-creatures/README.md`
- `src/app/discoveries/page.tsx`
- `src/app/components/discovery/CreatureEntry.tsx`
- `src/app/components/discovery/DiscoveryAlbumProgress.tsx`
- `src/app/styles/discoveries.css`
- `src/lib/i18n.ts`

## 実装要件

- 児童向け文言を「おとのいきもの」「おとのずかん」「であった」に変更する。
- 未発見では同じportrait画像をCSSでシルエット化する。
- 未発見では正式名、色、説明をアクセシブルネームからも漏らさない。
- 発見済みでは全身、名前、性格、理由、音楽のことば、発見日を表示する。
- 「音楽のことば」は常時表示し、開閉UIを設けない。
- light/dark、desktop/tablet/mobile、`prefers-reduced-motion` に対応する。
- 画像読込失敗時にもテキスト内容へ到達できる。

## 対象外

- 発見判定の変更
- 初回発見モーダルの変更
- 相棒選択
- DBマイグレーション

## 合格条件

- 既存の発見済みデータがそのまま生き物の解放状態として表示される。
- 未発見と発見済みが色だけでなく形・文言でも区別できる。
- 未発見の名前をDOMやalt文から読み取れない。
- 日英切替ですべての新規文言が切り替わる。
- 各ブレークポイントとキーボード操作を手動確認している。
- lint、型検査、関連テストが通る。

## 実装・確認記録

- `ac3de06`: 旧カードUIを図鑑エントリへ置き換え、未発見時に正式名とalt文を
  HTMLへ出さないテストを追加した。
- `fc77b55`: 同じportraitをlight/dark別の単色シルエットとして表示し、
  900px以上は3列、600〜899pxは2列、599px以下は1列にした。
- localhostで未発見8体と発見済みのミッツを表示し、全身、名前、性格、
  「どうして あらわれたの？」、常時表示の「音楽のことば」、発見日を確認した。
- 1280px、768px、390pxで列数と横方向のはみ出しがないことを確認した。
- アカウントメニューから英語へ切り替え、見出し、状態、説明、alt文が切り替わることを
  確認した。
- キーボードのTab操作で、モバイル幅でもヘッダーの「つくる」導線へフォーカスが
  到達することを確認した。図鑑エントリ自体は閲覧要素として不要なTab停止点を持たない。
- 画面上部3体の画像を先読みし、確認中に検出したLCP警告が再発しないことを確認した。
